#todo

auto copy ssh key from dropbox
open apps (dropbox, cloud, ...)
differences between install / update
add silent mode
add the symlinking for sublime configuration
try mackup (https://github.com/lra/mackup)
test if /usr/local/bin/zsh exists already at the end of /etc/shells file and don't add it if so
do an update and an install process
include the prezto update process (https://github.com/sorin-ionescu/prezto#updating)